<!doctype html>
<html>
  <head>
	<meta charset="utf-8">
	<title>{{ title }}</title>
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<style> body{font-family:sans-serif;padding:2rem;line-height:1.5} code{background:#f3f3f3;padding:.2rem .4rem;border-radius:4px} </style>
  </head>
  <body>
	<h1>{{ title }}</h1>
	<p>{{ msg }}</p>
	<p>PHP: <code>{{ constant('PHP_VERSION') }}</code></p>
  </body>
</html>
